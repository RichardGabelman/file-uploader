<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Update Folder</title>
</head>
<body>
  <h1>Update Folder</h1>
  <% if (typeof errors !== 'undefined' && errors.length > 0) {%>
    <ul>
      <% errors.forEach(function(error) { %>
        <li><%= error.msg %></li>
      <% }); %>
    </ul>
  <% } %>
  <form action="/folders/<%= folder.id %>/update" method="POST">
    <label for="name">Name</label>
    <input id="name" name="name" placeholder="Name" type="text" value="<%= folder.name %>"/>
    <button type="submit">Submit</button>
  </form>
</body>
</html>